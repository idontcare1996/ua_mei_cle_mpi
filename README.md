# Acoustic Modelling (Using MPI)

Large Scale Computation Assignment using MPI to acoustically model a room.

Compile with:  ```mpicc -o rmconf rmconf.c -lm -g -O2```.

Run with ```./rmconf```.

Follow the on-screen instructions to model the room.